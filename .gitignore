cat > .gitignore << 'EOF'
# API Keys and secrets — NEVER push these
.env
*.env
config.py
secrets.py

# Python cache — not needed
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# OS files — not needed
.DS_Store
Thumbs.db
EOF